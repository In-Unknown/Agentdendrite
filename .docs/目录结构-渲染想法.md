c:\D\01-Projects\Agentdendrite\目录结构-渲染想法.md
src
├── features/                # ← 你的“全能文件夹”，所有功能模块都在这
│   ├── layout/              # 布局系统模块
│   │   ├── models/          # 数据模型 (PageLayout.ts)
│   │   ├── stores/          # 逻辑与状态 (useLayout.ts)
│   │   └── views/           # 组件 (FreeFolder.vue, TagFolder.vue)
│   │
│   ├── editor/              # 未来可能的编辑器模块
│   │   ├── models/
│   │   ├── stores/
│   │   └── views/
│   │
│   └── file-explorer/       # 未来可能的文件树模块
│       ├── models/
│       ├── stores/
│       └── views/
│
├── shared/                  # ← 真正的“通用文件夹”
│   ├── components/          # 跨模块通用的原子组件 (Button, Input, Icon)
│   ├── utils/               # 工具函数
│   └── styles/              # 全局样式
│
├── views/                   # 路由层面的页面 (如 AiChat 整个大页面)
│   └── tabs/
│
├── App.vue
└── main.ts

